import { Canvas } from "@react-three/fiber";
import { io } from "socket.io-client";
// const socket = io("ws://localhost:3001");
// socket.on("connect", () => {
//   console.log(`connected: ${socket.id}`);
// });
function App() {
  return <Canvas>qwe</Canvas>;
}

export default App;
